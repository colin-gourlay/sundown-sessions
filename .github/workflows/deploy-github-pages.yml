name: Deploy to GitHub pages

on:
  push:
    branches:
      - main
    paths:
      - src/**
      - .github/workflows/deploy-github-pages.yml
  pull_request:
    paths:
      - src/**
      - .github/workflows/deploy-github-pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.146.5
      HUGO_CACHEDIR: /tmp/hugo_cache
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
      - name: Install Dart Sass (no snap)
        env:
          SASS_VERSION: 1.77.8
        run: |
          set -euo pipefail
          curl -fsSL -o "${{ runner.temp }}/dart-sass.tar.gz" \
            "https://github.com/sass/dart-sass/releases/download/${SASS_VERSION}/dart-sass-${SASS_VERSION}-linux-x64.tar.gz"
          tar -xzf "${{ runner.temp }}/dart-sass.tar.gz" -C "${{ runner.temp }}"
          sudo mv "${{ runner.temp }}/dart-sass" /opt/dart-sass
          sudo ln -sf /opt/dart-sass/sass /usr/local/bin/sass
          sass --version
      - name: Checkout source using v7.0.0
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          submodules: recursive
      - name: Cache Hugo build artefacts
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            src/resources/_gen
            /tmp/hugo_cache
          key: hugo-${{ runner.os }}-${{ hashFiles('src/config/**', 'src/layouts/**', 'src/content/**', 'src/themes/**', 'src/assets/**') }}
          restore-keys: |
            hugo-${{ runner.os }}-
      - name: Setup pages using v6.0.0
        id: pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"
      - name: Build with Hugo
        env:
          HUGO_ENVIRONMENT: githubPages
          HUGO_ENV: githubPages
        run: |
          hugo \
            --source src \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"
      - name: Upload artifact using v5.0.0
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: ./src/public

  deploy:
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment_retry_2.outputs.page_url || steps.deployment_retry_1.outputs.page_url || steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages using v5.0.0
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
        continue-on-error: true
        with:
          timeout: 600000
      - name: Wait before first retry of GitHub Pages deployment
        if: steps.deployment.outcome == 'failure'
        run: sleep 60
      - name: Retry GitHub Pages deployment (attempt 2 of 3) using v5.0.0
        if: steps.deployment.outcome == 'failure'
        id: deployment_retry_1
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
        continue-on-error: true
        with:
          timeout: 600000
      - name: Wait before second retry of GitHub Pages deployment
        if: steps.deployment.outcome == 'failure' && steps.deployment_retry_1.outcome == 'failure'
        run: sleep 120
      - name: Retry GitHub Pages deployment (attempt 3 of 3) using v5.0.0
        if: steps.deployment.outcome == 'failure' && steps.deployment_retry_1.outcome == 'failure'
        id: deployment_retry_2
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
        with:
          timeout: 600000
